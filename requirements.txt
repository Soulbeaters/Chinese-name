# Основные зависимости / 核心依赖
# ChineseNameProcessor v2.0.0

# Опциональные зависимости / 可选依赖
pypinyin>=0.44.0  # Поддержка конверсии пиньинь / 拼音转换支持

# Зависимости разработки / 开发依赖
pytest>=6.0      # Фреймворк тестирования / 测试框架
pytest-cov       # Тестирование покрытия / 覆盖率测试
black            # Форматирование кода / 代码格式化
flake8           # Проверка качества кода / 代码质量检查