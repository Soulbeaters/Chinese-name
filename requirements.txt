# 核心依赖 / Core Dependencies
# ChineseNameProcessor v2.0.0

# 可选依赖 / Optional Dependencies
pypinyin>=0.44.0  # 拼音转换支持 / Pinyin conversion support

# 开发依赖 / Development Dependencies
pytest>=6.0      # 测试框架 / Testing framework
pytest-cov       # 覆盖率测试 / Coverage testing
black            # 代码格式化 / Code formatting
flake8           # 代码质量检查 / Code quality checking